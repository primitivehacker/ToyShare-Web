## Initial Setup

This requires the api to be set up and running locally, or the api to be pushed live somewhere.

clone the repo

    cd path-to-repo
    yarn install
    or
    npm i
    npm run start